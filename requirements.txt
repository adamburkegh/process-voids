pm4py >= 2.7, <= 2.8
rustxes >= 0.2.10
matplotlib >= 3.11.1
# skipalignments and pm4py_config are not yet published/tagged
# anywhere real. Use `pip install -e .` instead (pyproject.toml
# points at local ../skip-alignments and ../pm4py-config checkouts).
# Once they're pushed and tagged, replace with real pins here too.
